# tuples are immutable that is they can't be modified or changed
coordinates = (4, 5)
print(coordinates)
print(coordinates[0])

# we can create a list of tuples too
numbers = [(2,4), (7, 8), (9,0)]
print(numbers)
print(numbers[1])